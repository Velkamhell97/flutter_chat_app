enum EnvironmentMode {
  production,
  development,
}

enum ServerStatus {
  online,
  offline,
  connecting
}

enum AuthType {
  login,
  register
}

enum AuthMethod {
  email,
  phone
}

enum MessageStatus {
  unsent,
  sent,
  received,
  read
}

enum Camera {
  rear,
  front,
}

enum MediaType {
  image,
  video,
  file
}

enum EditionType {
  crop,
  paint,
  emoji
}